server:
  port: ${EUREKA_SERVER_PORT:8761}
eureka:
  instance:
    hostname: ${EUREKA_HOSTNAME:localhost}
  client:
    register-with-eureka: ${EUREKA_REGISTER:false}
    fetch-registry: ${EUREKA_FETCH:false}
  server:
    wait-time-in-ms-when-sync-empty: ${EUREKA_WAIT_TIME:5}
